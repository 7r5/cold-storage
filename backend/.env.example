# Local example. Copy to .env and edit.
PORT=4000
NODE_ENV=development

# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:pg@localhost:5432/cold_chain_control?schema=public"

# Allowed origin for CORS / socket.io (comma separated for multiple)
CORS_ORIGIN=http://localhost:5173
